/*
 * Appends message to the message storing file
 * Params:
 *        role {str} - either "hider" or "seeker"
 *     content {str} - the text content of the message
 */
function sendMessage(role, content){}

/*
 * Gets the messages from server
 * Returns {list of obj} - the messages
 *     obj{
 *         sender {str}
 *         content {string}
 *     }
 */
function getMessages(){}